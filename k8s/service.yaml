apiVersion: v1
kind: Service
metadata:
  name: java-app-service
spec:
  selector:
    app: java-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080   # access via minikube ip:30080
